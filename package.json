{
  "name": "nouns-flows",
  "version": "0.1.0",
  "license": "GPL-3.0",
  "scripts": {
    "prettier:check": "prettier --check 'src/**/*.sol' 'test/**/*.sol' 'script/**/*.sol'",
    "prettier:write": "prettier --write 'src/**/*.sol' 'test/**/*.sol' 'script/**/*.sol'",
    "test": "forge test -vv",
    "dev": "forge test --watch -vvv",
    "test-gas": "forge test --gas-report",
    "build:sizes": "forge build --sizes",
    "storage-inspect:check": "./script/storage-check.sh check ERC20VotesMintable",
    "storage-inspect:generate": "./script/storage-check.sh generate ERC20VotesMintable"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "prettier-plugin-solidity": "^1.3.1"
  }
}
